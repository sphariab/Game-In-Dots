const styles = theme => ({
  wrapper: {
    display: "flex",
    justifyContent: "center",
    fontFamily: "Open Sans, sans-serif",
  },
  mainContent: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  }
});

export default styles
